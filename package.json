{
  "name": "casinocoin-libjs-bundle",
  "version": "2.0.0",
  "description": "Casinocoin monorepo encapsulating principle Javascript libraries.",
  "repository": {
    "type": "git",
    "url": "git://github.com/casinocoin/casinocoin-libjs.git"
  },
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=10.15.0"
  },
  "devDependencies": {
    "lerna": "2.11.0"
  },
  "scripts": {
    "lerna:bootstrap": "lerna bootstrap",
    "libjs:build": "npm run --prefix packages/@casinocoin/libjs build",
    "libjs:dev": "npm run --prefix packages/@casinocoin/libjs dev",
    "libjs:test": "npm run --prefix packages/@casinocoin/libjs test",
    "buildpack:casinocoin-libjs":"npm run --prefix packages/@casinocoin/libjs build && npm pack ./packages/@casinocoin/libjs && mv casinocoin-libjs-2.0.0.tgz dist/."
  }
}
